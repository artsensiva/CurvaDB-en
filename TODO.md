# TODO

- РЕШЕНО (step1, п.1): `benchmarks/step0.py`'s `MAX_ARC_POINTS`-кэп был
  обходом GPS-скачков в сырых данных. `src/traj/clean.py` теперь режет
  треки по разрывам (dt > 30с ИЛИ скорость > 70 м/с) до фиттинга/
  бенчмарков — устраняет причину, а не только следствие. `step0.py` сам
  не тронут (сравнение "было/стало" в step0_diagnostics.md и step1.md);
  все step1_*.py бенчмарки используют `load_clean_tracks()`.

- Порог приёмки честного фиттинга (step1, п.2, >= 99% треков с густой
  ошибкой <= tol) достигнут с запасом (100%, 585/585) — блокеров нет,
  записывать нечего.

- `benchmarks/step1_compression.py` и `step1_search.py` используют
  подвыборки очищенных треков (60 и все 585 соответственно) при разных
  seed — при желании прогнать на полном корпусе дольше (~4-15 минут на
  скрипт), но выводы (п.7) от размера выборки устойчивы (проверено на
  585/585 для search).

- step2 (`benchmarks/results/step2.md` §4): на грубом tol=20м DP+SED
  выигрывает у сплайна на ВСЕХ уровнях шума (включая sigma=0), хотя на
  tol=1-5м сплайн выигрывает — и это не объясняется неоптимальностью
  FITPACK (п.3). Причина не выяснена (кандидат: честная densify-стратегия
  `fit()` наращивает контрольные точки ради густой проверки МЕЖДУ
  1-секундными отсчётами независимо от формы поворота) — не исследовано
  дальше, т.к. не требовалось заданием.

- step3 (`benchmarks/results/step3.md` §2-3): K1-K3 не выполнены на
  15 треках. Два открытых наблюдения, не исследованных дальше (не
  требовалось заданием):
  - Разрыв между оракулом (15/15 достижимо на всех tol) и
    `fit_uniform`/`fit_adaptive` (0-53% на клетках с dt>=5с/sigma=5) —
    оракул фитится на ИСТИННУЮ плотную кривую, а не на редкие/шумные
    отсчёты, так что разрыв ожидаем, но его величина не разложена на
    вклад шума vs вклад разреженности отдельно.
  - При dt=15с ВСЕ методы (DP+SED и оба LSQ-сплайна) недостижимы на
    100% клеток сетки (§2) — кандидат-причина: при переменной скорости
    короткие резкие повороты (радиус 30-150м) на части из 15 треков
    целиком укладываются в один интервал между соседними 15-секундными
    отсчётами, так что ни один метод, видящий только эти отсчётки, не
    может восстановить поворот (ни выбором подмножества точек, ни МНК-
    сплайном) — не проверено на всех 15 треках по отдельности, не
    отделено от возможного вклада `search_min_params`-скана (8 точек,
    не непрерывный).
