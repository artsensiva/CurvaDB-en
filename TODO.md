# TODO

- `benchmarks/step0.py`: дискретизация сплайна с шагом ~tol по длине дуги
  (`_arclength_resample`, `MAX_ARC_POINTS`) захардкожена с кэпом в 3000
  точек. Без кэпа один трек с аномальным GPS-скачком (>3 км между соседними
  точками, ~1065 км суммарной длины дуги) порождает сотни тысяч точек и
  валит дискретную Фреше по памяти (n×m double-матрица). Кэп — обход, а не
  фикс причины (данные GeoLife сами по себе содержат такие скачки). Если
  понадобится честная arc-length дискретизация без кэпа — нужно сначала
  резать треки по большим временным/пространственным разрывам ещё в
  `src/traj/io.py`, это не делалось (не было в ТЗ шага 1).
